models:
  Book:
    title: string
    description: string
    year: int
    publisher: string
    date: date
    author_id: id
  Author:
    name: string
    gender: string
    relationships:
      hasMany: Book


controllers:
  # ...
